{
  "name": "jukebox",
  "version": "1.0.0",
  "private": true,
  "description": "Party jukebox: guests request songs from their phones, the host's Spotify plays them.",
  "type": "module",
  "engines": { "node": ">=20.6" },
  "scripts": {
    "start": "node --env-file=.env server.js"
  }
}
